{
	"name": "pasobi",
	"version": "1.0.0",
	"private": true,
	"workspaces": ["application", "domain", "infra"],
	"scripts": {
		"build": "cd infra && npm run build",
		"dev": "cd infra && npm run dev",
		"start": "cd infra && npm run start"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.8.2",
		"bun-types": "latest"
	},
	"peerDependencies": {
		"typescript": "latest"
	}
}
